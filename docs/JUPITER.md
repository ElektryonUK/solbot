# Jupiter API

- Default base is `https://lite-api.jup.ag/swap/v1`. Pro users can set `JUP_BASE=https://api.jup.ag/swap/v1`.
- For environments without egress or DNS, set `OFFLINE_QUOTES=true` to use mock quotes during dry-run.
